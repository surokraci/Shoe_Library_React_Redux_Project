import { GIFT_ADD, GIFT_DELETE } from "../actions/GiftAction";

export const GiftReducer = (state = [], action) =>{
    console.log(action);
    switch(action.type){
        default:
            return state
    }
}
